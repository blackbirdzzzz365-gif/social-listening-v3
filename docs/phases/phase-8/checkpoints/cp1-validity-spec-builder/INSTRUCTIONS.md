# CP1 — Validity Spec Builder

1. Xac dinh contract `validity_spec` cho Phase 8
2. Build logic compile tu topic + clarification history + keyword/context package + plan intent
3. Persist hoac attach spec version vao context/run
4. Them tests cho schema va generation path
