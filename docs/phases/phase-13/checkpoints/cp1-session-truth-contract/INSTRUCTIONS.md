# Instructions

- Xac dinh state model truth-first cho session browser.
- Khoa invariant de `EXPIRED` khong the van runnable.
- Them field/enum neu can cho `action_required` va `health_block_reason`.
