# CP5 — Clean Payload + AI Guardrail

1. Xay dung clean payload stage sau scoring/expansion
2. Implement normalize whitespace, strip UI noise, duplicate detection, quality flags
3. Gate AI queue theo `strict` va `balanced`
4. Them test cho accepted-only va uncertain band behavior
