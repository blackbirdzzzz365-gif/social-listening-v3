# CP1 — Retrieval Profile Builder

1. Thiet ke contract `retrieval_profile` tu topic + keyword map
2. Build logic tao `query_families` va `source_hints`
3. Dam bao planner hoac service lien quan persist duoc metadata can thiet cho phase sau
4. Them test cho shape va query-family coverage
